<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>NodeMailerApp - Email Form</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet"/>
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <div class="container">
      <h1 class="title">Email Sender</h1>
      <p class="description">
        Easily send emails with NodeMailerApp. Enter the recipient's email and your message below, then click "Send Email" to get started.
      </p>
      <form
        action="/send-email"
        method="POST"
        class="email-form"
        id="email-form"
      >
        <input
          type="email"
          name="email"
          id="email"
          placeholder="Enter recipient email"
          required
        />
        <textarea
          name="message"
          id="message"
          rows="5"
          placeholder="Enter your message"
          required
        ></textarea>
        <button type="submit" id="submit-button">Send Email</button>
      </form>

      <% if (typeof status !== 'undefined' && status && typeof message !==
      'undefined' && message) { %>
      <div class="status-message <%= status %>"><%= message %></div>
      <% } %>

      <p class="copyright">&copy; 2025 NodeMailerApp. All rights reserved.</p>
    </div>
  </body>
</html>